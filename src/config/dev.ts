/**
 * Single root flag for dev-only tooling.
 *
 * Defaults to React Native's `__DEV__` (true in debug builds, false in release).
 * Flip to `false` to hide the in-app dev panel even during development.
 */
export const DEV_MODE: boolean = __DEV__;
